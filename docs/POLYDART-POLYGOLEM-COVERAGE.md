# Polydart-Polygolem Coverage Matrix

This matrix tracks Polydart completion against upstream Polygolem. Polygolem is
the older-brother protocol source of truth; Polydart keeps twin parity for
public protocol surfaces, request/response shapes, fixtures, feature checklist,
safety gates, and user-facing capabilities. Polydart may intentionally diverge
on Dart/Flutter SDK custody architecture as described in
`docs/adr/0001-wallet-mediated-eoa-signing.md`, but each divergence must be
explicit in this matrix.

- Canonical upstream source: `polygolem/`
- Last scaffolded Polygolem commit: `2b7cde7`
- Last fidelity sync commit: `fb092d8b`
- Upstream HEAD (pending port): `12f5cecd`
- Scaffold command: `python3 skills/polydart/scripts/polygolem_inventory.py --root .`
- Upstream delta after `21f1982`: Gamma comment profiles normalization, market
  trades Data API, batch orderbooks exposure, and market metadata are ported.
  Polygolem `91876cf` added `pkg/orderfills`; Polydart covers public models,
  validation, reader interfaces, and read-only Polygon JSON-RPC log decoding
  with mocked RPC tests. Polygolem `fb092d8b` added Wallet Intelligence V1;
  Polydart covers read-only DTOs, shrinkage scoring, Data API-backed dossiers,
  leaderboard rows, alerts, and market-flow summaries.
- Upstream delta after `fb092d8b`: Polygolem `3dfca41c` added CLOB
  `MarketOutcome` (condition-ID resolution with Gamma fallback) and
  `SmartWalletTrades` (public trades annotated with watched-wallet source
  metadata); both are now ported to Polydart as `ClobClient.marketOutcome` and
  `DataApiClient.smartWalletTrades` with mocked HTTP tests (the Gamma fallback
  exercised against a loopback server). Polygolem `766b58be` added read-only
  agent surfaces, of which `pkg/openapi` (a read-only OpenAPI 3.1 spec emitter)
  is now ported as `openApiSpec()` and `pkg/rfq` (RFQ request/quote DTOs and
  validation, live submission intentionally unimplemented) is now ported as
  `lib/src/rfq` with a safety-gated `RfqClient.submit` that validates then
  refuses. `pkg/mcp` is now ported as a no-credential read-only
  `McpServer`/`safeMcpTools` surface. `pkg/signers` is represented by
  Polydart's EOA-compatible `PolydartSigner` seam plus wallet-provider,
  explicit local/private-key EOA, and optional HTTP/KMS/Turnkey adapter
  boundaries. Polygolem
  `12f5cecd` added the opportunity-scan workflow (`internal/workflows/
  opportunities`), `fixtures/conformance` byte-level golden vectors
  (`builder_headers`, `clob_auth_v2`, `ctf_calldata`, `order_v2_poly1271`), and
  `internal/stream` connection/throughput stats. The conformance fixtures are
  mirrored under `test/fixtures/conformance` with Dart tests for builder/L2
  headers, local-private-key L1 EOA signatures, Order V2 struct hashes, and CTF
  calldata. Opportunity scanning is now
  ported as `OpportunityRunner` (wide spread, low-liquidity/high-volume, new,
  closing-soon, negative-risk, and crypto-5m scans). Stream stats are now
  ported as `StreamStats`/`StreamStatsSnapshot` with market/user client
  snapshots. The wallet-intelligence E2E test (`54b161e1`) is already mirrored
  in `test/e2e`.

Status values: `implemented`, `partial`, `missing`, `intentional Dart
divergence`, `not applicable`.

`implemented` means behavior, public API/export story, tests or fixtures,
safety gates where relevant, user docs where needed, and upstream commit are
all accounted for.

## Priority Blockers

| Feature | Polygolem Path | Polydart Path | Status | Tests/Fixtures | Safety Review | Upstream Commit | Next Action |
|---|---|---|---|---|---|---|---|
| EOA signer compatibility | internal/auth, internal/wallet | lib/src/auth, lib/src/wallet, lib/src/signers | implemented | test/auth, test/wallet, test/signers | required | 2b7cde7 | ADR 0001 requires both wallet-provider EOA signers (MetaMask/Reown-compatible) and explicit opt-in private-key EOA signers; `LocalEoaSigner` implements both `WalletSigner` and `PolydartSigner` with deterministic Ethereum signatures and redaction/validation coverage |
| Deposit-wallet live semantics | internal/wallet, internal/relayer, internal/clob | lib/src/wallet, lib/src/relayer, lib/src/clob, lib/src/credentials, lib/src/orders, lib/src/funding | partial | test/wallet, test/relayer, test/clob, test/credentials, test/orders, test/funding | required | 5220881a | Deposit-wallet readiness, limit-order placement, batch limit-order placement, buy/sell market-order placement, cancellation auth, EOA pUSD funding route planning, and post-funding confirmation now cover sigtype-3 approval/funding checks, ERC-7739 wrapped signing, deposit-wallet maker/signer fields, EOA-bound CLOB HMAC auth, direct `pUSD.transfer` wallet transaction planning, wallet-submitted tx receipt polling, CLOB collateral refresh, market amount truncation, book price discovery, typed CLOB live error bodies, relayer deployed-address mismatch blocking, and Polygolem-matched batch/cancel request shapes; continue Flutter example/demo wiring |
| EIP-712 / ERC-7739 / POLY_1271 signing | internal/auth, internal/clob/orders.go | lib/src/auth, lib/src/orders | partial | test/auth, test/orders | required | 2b7cde7 | Regular and neg-risk order/ETH-712/ERC-7739/POLY_1271 byte-level vectors covered; wallet-provider signatures normalize compact `v=0/1` and reject malformed recovery IDs before order, POLY_1271, and enable-trading signing surfaces consume them; continue authenticated edge-case parity |
| Relayer and enable-trading surfaces | pkg/relayer, pkg/enabletrading, internal/enabletrading | lib/src/relayer, lib/src/enabletrading, lib/src/wallet | partial | test/relayer, test/enabletrading, test/wallet | required | 2b7cde7 | Relayer V2 API-key headers and `createdAt`/`updatedAt` key diagnostics, typed relayer error DTO/code parsing, deployed response aliases with bool/string/numeric drift, nonce map/scalar/alias drift, transaction object/list responses with `transactionID`/`transactionId`/snake aliases and numeric scalar fields, encoded read query parameters, poll defaults, allowlist classification, Polygolem-matched wallet-batch deadline clamping, wallet-provider and explicit private-key EOA enable-trading signatures, and adapter approval-call builder covered; live submission UX checklist documented in `docs/END-USER-GUIDE.md`; continue endpoint parity |
| CLOB write responses and order results | internal/clob, pkg/orderresults | lib/src/clob, lib/src/orders, lib/src/orderresults | partial | test/clob, test/orders, test/orderresults | required | 5220881a | Order response casing including `orderID`/`orderId`/`order_id`, bool/string/numeric success flags, numeric/string amount, numeric/string list values for `transactionHash`/`transaction_hash`, `transactionHashes`/`transactionsHashes`/`transaction_hashes`, `tradeIDs`/`tradeIds`/`trade_ids`, and `errorMsg`/`error_msg` variants, cancel response casing including scalar/list numeric/string `canceled` and map/list/scalar `not_canceled`/`notCanceled` values, batch create list and wrapped object responses, buy/sell market-order placement, EOA-bound batch/cancel `POLY_ADDRESS`, cleaned cancel IDs, cancel body casing, heartbeat, typed live error DTO fields, builder-trades read, and order-results report builder covered; continue authenticated read/write edge cases |
| Data API live V2 field shapes | internal/dataapi, pkg/data | lib/src/dataapi | implemented | test/dataapi | not_required | f4d0443 | Current-position market/side/unrealizedPnl fields plus snake/camel aliases and numeric-string tolerance, closed-position and trade numeric-string fields, activity `asset_id`/`assetId`, holder/proxyWallet/amount numeric-string fields, total-value default user/object-list and numeric-string decoding, open-interest `value`/`open_value` numeric-string decoding, markets-traded `markets_traded`/`traded` numeric-string decoding, volume numeric-string decoding, leaderboard aliases and numeric-string decoding, aggregate object/list decoding, and current endpoint routing covered; monitor upstream |

## Surface Inventory

| Feature | Polygolem Path | Polydart Path | Status | Tests/Fixtures | Safety Review | Upstream Commit | Next Action |
|---|---|---|---|---|---|---|---|
| pkg/bridge | polygolem/pkg/bridge | lib/src/bridge | implemented | test/bridge | not_required | 2b7cde7 | HTTP-only bridge client covered, including supported-assets numeric/string drift, deposit-status numeric/string drift for IDs/base units/millis, and quote numeric-string drift |
| pkg/builder | polygolem/pkg/builder | lib/src/builder | implemented | test/builder | not_required | 2b7cde7 | Local and remote builder signer surfaces covered |
| pkg/clob | polygolem/pkg/clob | lib/src/clob, lib/src/credentials | partial | test/clob, test/credentials | required | 2b7cde7 | Read-side CLOB market, market-by-token, book/batch-book numeric-string and camel alias tolerance, price, batch price, midpoint, spread, tick-size numeric-string and camel alias tolerance, neg-risk metadata numeric-string/camel alias tolerance, fee-rate and fee-details numeric-string/camel alias tolerance, last-trade, price-history numeric-string/alias tolerance, public trades, single/batch scoring bool drift tolerance, builder-trades and rewards numeric-string/camel alias tolerance, wallet-mediated CLOB L2 create/derive, balance-allowance asset-type/signature-type request normalization and numeric/string response decoding, authenticated order `type`/`order_type`, string/numeric `signature_type`, public/internal numeric/string amount/timestamp/associate-trade fields plus snake/camel aliases, authenticated trade public/internal numeric/string amount/timestamp fields plus snake/camel aliases including `assetId`/`createdAt`, and builder-fee key orchestration and `createdAt`/`updatedAt` casing covered; Polygolem `3dfca41c` read helpers `MarketOutcome` (condition-ID resolution with Gamma fallback) and `SmartWalletTrades` (watched-wallet-annotated public trades) ported as `ClobClient.marketOutcome` (in `lib/src/dataapi` for smart-wallet trades); continue authenticated write parity |
| pkg/contracts | polygolem/pkg/contracts | lib/src/contracts | implemented | test/contracts | not_required | 2b7cde7 | Contract registry and eth_getCode readiness covered |
| pkg/ctf | polygolem/pkg/ctf | lib/src/ctf | implemented | test/ctf | not_required | 2b7cde7 | CTF calldata and ID helpers covered |
| pkg/data | polygolem/pkg/data | lib/src/dataapi | implemented | test/dataapi | not_required | f4d0443 | Current Data API wrapper methods, endpoints, current-position market/side/unrealizedPnl fields plus snake/camel aliases and numeric-string tolerance, activity `asset_id`/`assetId`, holder proxyWallet/amount numeric-string fields, total-value default user/object-list and numeric-string decoding, open-interest `value`/`open_value` numeric-string decoding, markets-traded `markets_traded`/`traded` numeric-string decoding, leaderboard aliases with numeric-string decoding, and DTO decoding covered; `SmartWalletTrades` (`3dfca41c`) ported as `DataApiClient.smartWalletTrades` with dedupe-by-trade-id; monitor upstream |
| pkg/enabletrading | polygolem/pkg/enabletrading | lib/src/enabletrading | partial | test/enabletrading | required | 2b7cde7 | Wallet-mediated and explicit private-key EOA ClobAuth plus approval batch planning covered; live submission stays app-owned/gated |
| pkg/experimental | polygolem/pkg/experimental |  | not applicable |  | not_required | 2b7cde7 | Revisit only if promoted upstream |
| pkg/funding | polygolem/pkg/funding | lib/src/funding | intentional Dart divergence | test/funding | required | 2b7cde7 | EOA pUSD balance route planning and direct wallet transaction request covered; raw live transfer submission intentionally omitted |
| pkg/gamma | polygolem/pkg/gamma | lib/src/gamma | implemented | test/gamma | not_required | 2b7cde7 | Health, markets, events, series, tags, comments, teams, sports metadata/types, public profiles, market-by-token, keyset pagination, profile creation helper, routing, params, and DTO decode coverage are in place |
| pkg/intel | polygolem/pkg/intel, polygolem/internal/intel | lib/src/intel | implemented | test/intel, test/e2e | not_required | fb092d8b | Wallet Intelligence V1 DTOs, shrinkage win-rate scoring, ROI, explainable `scoreWallet`, closed-position-authority wallet dossiers, Data API leaderboard rows, threshold alerts, market-flow summaries, source provenance, conflict/warning statuses, `Polydart.intel`, `UniversalClient` delegation, and local/mock E2E parity covered; output language remains candidate-only and not a finding of misconduct |
| pkg/marketdata | polygolem/pkg/marketdata | lib/src/marketdata | implemented | test/marketdata | not_required | 2b7cde7 | Market-data tracker covered |
| pkg/marketresolver | polygolem/pkg/marketresolver | lib/src/marketresolver | implemented | test/marketresolver | not_required | 2b7cde7 | Slug/id resolution, CLOB token parsing, crypto window slugging, strict/best-effort up/down token resolution, status mapping, and token validation covered |
| pkg/mcp | polygolem/pkg/mcp | lib/src/mcp | implemented | test/mcp | not_required | 766b58be | Read-only Model Context Protocol server ported as `McpServer`, `safeMcpTools`, and `newReadOnlyMcpHandlers` (`mcp.go`/`handlers.go`); exposes no-credential health/discover/data/orderbook/marketdata tools, rejects unsafe names, fails safely when handlers are unconfigured, and excludes trade/sign/approval/withdraw surfaces. `sdk_handlers.go` is now ported as `newSdkReadOnlyMcpHandlers` (wires concrete `GammaClient`/`DataApiClient`/`ClobClient` with nil-skipping and base-wins adapter merge) and `newMarketDataSnapshotMcpHandler` (read-only `MarketDataTracker` snapshot). `Position` and `SearchResponse` gained `toJson` (canonical Go keys; `Event` re-emits its retained raw payload) so the `data_positions` and `discover_search` tools marshal cleanly end-to-end through `tools/call` |
| pkg/openapi | polygolem/pkg/openapi | lib/src/openapi | implemented | test/openapi | not_required | 766b58be | Read-only OpenAPI 3.1 spec emitter ported as `openApiSpec()`; same six read-only paths (`/health`, `/diag`, `/discover/search`, `/data/positions`, `/orderbook/{token_id}`, `/marketdata/snapshot`), sorted query params, path-vs-query distinction, and mutating-wording exclusion covered |
| pkg/rfq | polygolem/pkg/rfq | lib/src/rfq | implemented | test/rfq | required | 766b58be | RFQ DTOs (`RfqRequest`/`RfqMetadata`/`RfqQuote`/`RfqResponse`), `validateRfqRequest` (market-or-token requirement, strict positive-decimal amount rejecting `1e3`/`1.2.3`/`0`, BUY/SELL side, strictly-future expiration), and JSON round-trips covered; `RfqClient.submit` validates then throws `SafetyException` (`liveDisabled`) — live submission intentionally omitted under ADR 0001, matching Go's `ErrSubmitUnsupported` |
| pkg/signers | polygolem/pkg/signers | lib/src/signers, lib/src/auth | implemented | test/signers | required | 766b58be | Signing seam ported as `PolydartSigner` with `WalletMediatedSigner`, `LocalEoaSigner`, and optional HTTP/KMS/Turnkey adapter boundaries. `LocalEoaSigner` supports explicit private-key EOA signing for testing/headless users and also satisfies existing `WalletSigner` APIs for MetaMask/Reown-compatible flows. |
| pkg/orderbook | polygolem/pkg/orderbook | lib/src/orderbook | implemented | test/orderbook | not_required | 21f1982 | ClobOrderBookReader (single + batch fetches), ValidationException on empty tokenId, empty-skip behavior, BookReader integration, and 5 mock HTTP tests covered; matches polygolem Reader/BatchReader interfaces |
| pkg/orderbook (bookreader) | polygolem/pkg/bookreader | lib/src/bookreader | implemented | test/bookreader | not_required | 2b7cde7 | BookReader compute class (sorted bids/asks, midpoint, spread, depth) covered |
| pkg/orderresults | polygolem/pkg/orderresults | lib/src/orderresults | implemented | test/orderresults | not_required | 2b7cde7 | Report builder covered; CLOB inclusion uses ApiKey reader instead of raw private key |
| pkg/orderfills | polygolem/pkg/orderfills | lib/src/orderfills | implemented | test/orderfills | not_required | 91876cf | Public Fill/Market/Query, validation, reader interfaces, default exchange filters, buy/sell log decoding, market filtering, timestamp lookup, and latest-block reader covered with mocked Polygon JSON-RPC tests |
| pkg/pagination | polygolem/pkg/pagination | lib/src/pagination | implemented | test/pagination | not_required | 2b7cde7 | Cursor/offset stream, collect, and batch helpers covered |
| pkg/plugins | polygolem/pkg/plugins | lib/src/plugins | implemented | test/plugins | not_required | 2b7cde7 | Plugin interfaces covered |
| pkg/relayer | polygolem/pkg/relayer | lib/src/relayer, lib/src/wallet | partial | test/relayer, test/wallet | required | 2b7cde7 | V2 auth headers, typed relayer error DTO/code parsing, deployed response aliases with bool/string/numeric drift, nonce map/scalar/alias drift, transaction response shapes with `transactionID`/`transactionId`/snake aliases and numeric scalar fields, encoded read query parameters, poll defaults, allowlist rejection classifier, wallet-batch deadline minimum/clamping, and adapter approval-call builder covered; continue endpoint parity |
| pkg/settlement | polygolem/pkg/settlement | lib/src/settlement | partial | test/settlement | required | 2b7cde7 | Read-only redeem discovery, call planning, readiness checks, and adapter approval-call builder covered; live relay submission gated out |
| pkg/stream | polygolem/pkg/stream | lib/src/stream | implemented | test/stream | required for user stream | 12f5cecd | Market stream default URL, config payload, reconnect resubscribe, dedupe, lifecycle events, authenticated user order/trade stream, and Polygolem `StreamStats`/`StreamStatsSnapshot` subscription/connection/throughput/duplicate/invalid counters covered; market/user clients expose JSON-friendly `stats` snapshots |
| pkg/types | polygolem/pkg/types | lib/src/types | partial | test/types | not_required | 2b7cde7 | Market DTO now covers all Polygolem `Market` JSON keys tracked in `pkg/types/gamma.go`, including market/UMA/range/format/sponsor/axis/creator/group fields, ready/funded timestamps, twitter card image, short outcomes, optimized image/icon DTOs, nested events, sports/game/liveness scalars, rewards, AMM/CLOB volume/liquidity splits, neg-risk fee fields, notification/review/comment/curation/score scalars, price-change metrics, competitive score, activation/book flags, chart/series colors, GMP display flags, neg-risk-other flag, and deployment flags/timestamps; Event DTO now covers `subtitle`, `subcategory`, `sortBy`, `isTemplate`, `templateVariables`, `createdBy`, `updatedBy`, `competitive`, `featuredImage`, `imageOptimized`, `iconOptimized`, `featuredImageOptimized`, `disqusThread`, `parentEvent`, `subEvents`, `isNew`, `cyom`, `showAllOutcomes`, `showMarketImages`, `automaticallyResolved`, `enableNegRisk`, `automaticallyActive`, `seriesSlug`, `eventWeek`, `score`, `elapsed`, `period`, `negRiskFeeBips`, live/ended state, finished timestamp, GMP chart mode, tweet/featured order counts, estimate flags/value, main-line fields, carousel map, deployment flags/timestamps, and game status; CLOB market DTO now covers `condition_id`/`c`, `question_id`/`q`, `game_start_time`/`gst`, `neg_risk_market_id`, `neg_risk_request_id`, and `notifications_enabled`; Profile DTO now covers Polygolem search-profile user/referral/audit/UTM/wallet/pseudonym/bio/optimized-image/certification fields; Series DTO now covers Polygolem scalar metadata (layout, flags, audit dates, comments, competitive, Pyth/CG IDs, score); Tag DTO now covers Polygolem published/audit metadata; Category, Collection, and EventCreator DTOs are typed and nested on Market/Event/Series where applicable; Team DTO now covers Polygolem audit timestamps; continue broad DTO field/casing comparison |
| pkg/universal | polygolem/pkg/universal | lib/src/universal | partial | test/universal | not_required | 2b7cde7 | Read-only universal facade covers Gamma, CLOB market-by-token/read methods including full neg-risk metadata, public trades, and builder-trades, Data API, market discovery, streams, and health checks; Polygolem universal builder-fee create/list/revoke delegation is covered, while Dart intentionally excludes authenticated raw-key write methods from the default public surface |
| pkg/wallet | polygolem/pkg/wallet | lib/src/wallet, lib/src/auth/create2.dart | partial | test/wallet, test/auth | required | 2b7cde7 | Proxy/Safe/Deposit CREATE2 derivation vectors align across Polygolem and Polydart; deposit-wallet readiness blocks when relayer-reported deployed addresses disagree with local derivation; continue protocol behavior across wallet-provider and explicit private-key EOA signers |
| internal/auth | polygolem/internal/auth | lib/src/auth | partial | test/auth, test/conformance | required | 12f5cecd | Regular and neg-risk signing vectors covered; Polygolem `12f5cecd` `fixtures/conformance` byte-level golden vectors (`builder_headers`, `clob_auth_v2`, `ctf_calldata`, `order_v2_poly1271`) are mirrored under `test/fixtures/conformance` with Dart tests for builder/L2 headers, local-private-key L1 EOA signatures, Order V2 struct hashes, and CTF calldata; wallet-provider compact recovery IDs normalize to Ethereum `v=27/28`, malformed recovery IDs fail closed, and explicit private-key EOA signing covers ClobAuth plus DepositWallet.Batch typed data; continue authenticated signer edge cases |
| internal/clob | polygolem/internal/clob | lib/src/clob, lib/src/credentials | partial | test/clob, test/credentials | required | 5220881a | CLOB L2 credential create/derive reuses one wallet-approved ClobAuth signature across fallback; builder-fee key creation/listing preserves partial readiness and `created_at`/`createdAt` plus `updated_at`/`updatedAt` casing; read-side market-by-token, order-book, tick-size, neg-risk metadata, fee-rate, and fee-details numeric-string/camel alias tolerance, public trades, single/batch scoring bool drift tolerance, builder-trades numeric-string/camel alias tolerance, balance-allowance asset-type/signature-type request normalization and numeric/string response decoding, authenticated order `type`/`order_type`, string/numeric `signature_type`, public/internal numeric/string amount/timestamp/associate-trade fields plus snake/camel aliases, authenticated trade public/internal numeric/string amount/timestamp fields plus snake/camel aliases including `assetId`/`createdAt`, analytics endpoints with rewards numeric-string/camel alias tolerance covered; buy/sell market-order placement, deposit-wallet batch placement, and cancellation use EOA-bound HTTP auth; order write response casing accepts upper-camel/lower-camel/snake order ID, bool/string/numeric success flags, numeric/string amount, numeric/string list values for `transactionHash`/`transaction_hash`, `transactionHashes`/`transactionsHashes`/`transaction_hashes`, `tradeIDs`/`tradeIds`/`trade_ids`, and error fields; cancel response casing accepts scalar/list numeric/string `canceled` and map/list/scalar `not_canceled`/`notCanceled` values; batch create accepts list and wrapped object responses; CLOB write 4xx bodies map to structured `ClobException.upstream`; continue authenticated read/write edge cases |
| internal/config | polygolem/internal/config | lib/src/config | implemented | test/config | not_required | 21f1982 | PolydartConfig with defaults, fromEnv (prefix support), copyWith, redacted toString, parseDuration, and 10 tests covered; YAML file loading intentionally omitted (CLI-only concern in Go) |
| internal/dataapi | polygolem/internal/dataapi | lib/src/dataapi | implemented | test/dataapi | not_required | f4d0443 | Current-position market/side/unrealizedPnl fields plus snake/camel aliases and numeric-string tolerance, closed-position and trade numeric-string fields, activity `asset_id`/`assetId`, holder proxyWallet/amount numeric-string fields, total-value default user/object-list and numeric-string decoding, open-interest `value`/`open_value` numeric-string decoding, markets-traded `markets_traded`/`traded` numeric-string decoding, volume numeric-string decoding, leaderboard aliases and numeric-string decoding, aggregate object/list fields, and endpoint names covered; monitor upstream |
| internal/enabletrading | polygolem/internal/enabletrading | lib/src/enabletrading | partial | test/enabletrading | required | 2b7cde7 | Wallet-provider typed-data builders and explicit private-key EOA signer compatibility for ClobAuth plus DepositWallet.Batch approval planning covered; live submission remains app-owned/gated |
| internal/errors | polygolem/internal/errors | lib/src/errors | implemented | test/errors | not_required | 21f1982 | All 20 ErrorCode values match 1:1 (NET-*, AUTH-*, CLOB-*, VAL-*, SAFETY-*, GAMMA-*); typed exception hierarchy (TransportException, AuthException, ClobException, ValidationException, SafetyException, GammaException) with 10 tests; ClobErrorResponse structured decoding covered |
| internal/execution | polygolem/internal/execution | lib/src/execution | implemented | test/execution | not_required | 21f1982 | Executor interface, PaperExecutor with Place/Cancel/GetOrder/ListOrders, PaperOrderEntry, PaperFillEntry, and 10 tests covered; maps executor semantics to polydart's paper/live/read-only boundaries |
| internal/gamma | polygolem/internal/gamma | lib/src/gamma | implemented | test/gamma | not_required | 2b7cde7 | Search/event/tag/series/profile/team/sports endpoints, params, profile creation helper, and DTO decode coverage are in place |
| internal/intel | polygolem/internal/intel | lib/src/intel | implemented | test/intel, test/e2e | not_required | fb092d8b | Read-only service composition over Data API current positions, closed positions, trades, market trades, holders, open interest, and trader leaderboard covered; partial/conflicted dossier behavior mirrors Polygolem; local/mock E2E tests exercise both Polydart and Polygolem services |
| internal/marketdiscovery | polygolem/internal/marketdiscovery | lib/src/marketdiscovery | implemented | test/marketdiscovery | not_required | 2b7cde7 | Gamma active-market search, full-event search enrichment, first-token CLOB enrichment, tick size, neg-risk, fee-rate, midpoint, spread, last trade, order book, and tolerant failure handling covered |
| internal/modes | polygolem/internal/modes | lib/src/modes | implemented | test/modes | not_required | 2b7cde7 | Parse defaults and live-gate validation covered |
| internal/orders | polygolem/internal/orders | lib/src/orders, lib/src/wallet | partial | test/orders, test/wallet | required | 5220881a | Deposit-wallet single and batch placement helpers, market amount semantics, validation, default expiration, salt building, price bounds, Polygolem-matched tick-floor rounding, and wallet-batch deadline clamping covered; continue authenticated edge-case parity |
| internal/output | polygolem/internal/output |  | not applicable |  | not_required | 2b7cde7 | CLI output formatting; no public SDK surface planned |
| internal/paper | polygolem/internal/paper | lib/src/paper | implemented | test/paper | not_required | 2b7cde7 | Local paper state covered |
| internal/polytypes | polygolem/internal/polytypes | lib/src/types | partial | test/types | not_required | 2b7cde7 | Market metadata DTO parity now covers all Polygolem `Market` JSON keys tracked in `pkg/types/gamma.go`, including market/UMA/range/format/sponsor/axis/creator/group fields, ready/funded timestamps, twitter card image, short outcomes, optimized image/icon DTOs, nested events, sports/game/liveness scalars, rewards, AMM/CLOB volume/liquidity splits, neg-risk fee fields, notification/review/comment/curation/score scalars, price-change metrics, competitive score, activation/book flags, chart/series colors, GMP display flags, neg-risk-other flag, and deployment flags/timestamps; Event metadata DTO parity expanded with subtitle, subcategory, sort hint, template flag/variables, creator/updater, competitive score, featured image, optimized image/icon/featured-image DTOs, Disqus thread, parent event, subEvents, new-event flag, CYOM flag, show-all-outcomes flag, show-market-images flag, automatically-resolved flag, enable-negative-risk flag, automatically-active flag, series slug, event week, score, elapsed, period, neg-risk fee bips, live/ended state, finished timestamp, GMP chart mode, tweet/featured order counts, estimate flags/value, main-line fields, carousel map, deployment flags/timestamps, and game status; CLOB market metadata now covers game start time, neg-risk IDs, notifications, and abbreviated provider shape; Profile metadata covers search-profile user/referral/audit/UTM/wallet/pseudonym/bio/optimized-image/certification fields; Series scalar metadata covers layout, flags, audit dates, comments, competitive, Pyth/CG IDs, and score; Tag metadata covers published/audit fields; Category, Collection, and EventCreator DTOs are typed and nested on Market/Event/Series where applicable; Team DTO covers audit timestamps; continue using upstream DTO fixtures to drive type parity |
| internal/preflight | polygolem/internal/preflight | lib/src/preflight | implemented | test/preflight | not_required | 2b7cde7 | Async preflight runner covered |
| internal/relayer | polygolem/internal/relayer | lib/src/relayer, lib/src/credentials, lib/src/wallet | partial | test/relayer, test/credentials, test/wallet | required | 2b7cde7 | SIWE cookie login, Relayer V2 API-key minting with `createdAt`/`updatedAt`, submit, auth headers, typed error DTO/code parsing, deployed response aliases with bool/string/numeric drift, nonce map/scalar/alias drift, object/list transaction responses with `transactionID`/`transactionId`/snake aliases and numeric scalar fields, encoded read query parameters, allowlist classification, readiness consumption, approval/funding readiness, and end-user live UX checklist covered; continue endpoint parity |
| internal/risk | polygolem/internal/risk | lib/src/risk | implemented | test/risk | not_required | 2b7cde7 | Local risk breaker covered |
| internal/rpc | polygolem/internal/rpc | lib/src/rpc | partial | test/rpc, test/funding | not_required | 2b7cde7 | Read-only code/approval/allowance/balance helpers covered; live transfer/swap submission still gated out |
| internal/stream | polygolem/internal/stream | lib/src/stream | implemented | test/stream | required for user stream | 12f5cecd | Message parsing, lifecycle events, reconnect resubscribe, custom-feature subscription, authenticated user order/trade stream, and `StreamStats` connection/throughput telemetry covered; `test/stream/stream_stats_test.dart` mirrors Polygolem `internal/stream/stats_test.go` |
| internal/telemetry | polygolem/internal/telemetry | lib/src/telemetry | implemented | test/telemetry | not_required | 2b7cde7 | Request/retry/rate-limit/circuit-open telemetry and redaction covered |
| internal/transport | polygolem/internal/transport | lib/src/transport | implemented | test/transport | not_required | 21f1982 | CircuitBreaker (3-state closed/open/halfOpen, maxFailures=5, resetTimeout=60s, halfOpenMaxRequests=3), RateLimiter (token bucket, tryAcquire/acquire/stop), HttpTransport (GET/POST with retry on 5xx/429, no-retry on POST/4xx, circuit breaker + rate limiter integration, timeout, redaction), and 38 tests covered; matches polygolem behavior |
| internal/wallet | polygolem/internal/wallet | lib/src/wallet, lib/src/auth/create2.dart | partial | test/wallet, test/auth | required | 2b7cde7 | Proxy/Safe/Deposit CREATE2 derivation vectors align across Polygolem and Polydart; deploy/status readiness fails closed when relayer-reported deployed addresses disagree with deterministic derivation; continue deploy/status/batch semantics across wallet-provider and explicit private-key EOA signers |
| internal/workflows | polygolem/internal/workflows | composed public SDK modules | partial | test/* by mapped module | mixed | 12f5cecd | Go workflow packages exist to decouple Cobra CLI commands from SDK adapters. Polydart does not mirror the workflow folder; mapped behavior lives in typed SDK modules (`marketresolver`, `marketdiscovery`, `orderbook`, `marketdata`, `dataapi`, `settlement`, `wallet`, `paper`, `stream`, `universal`, `intel`, and `opportunities`). Polygolem `internal/workflows/opportunities` is ported as read-only `OpportunityRunner` with market-listing scans and crypto-5m event/pricer enrichment; port other dedicated Dart workflows only if upstream adds product behavior not already represented by a public SDK module. |
| cli/* | polygolem/internal/cli |  | not applicable |  | mixed | 2b7cde7 | Port behavior only when it maps to public SDK APIs |

## Tracked Non-Protocol Cleanup

| Item | Status | Next Action |
|---|---|---|
| Retire legacy extra Polygolem checkout compatibility | implemented | Canonical upstream is a read-only local `polygolem/` checkout when protocol work needs upstream evidence |
| Port upstream conformance golden vectors (`fixtures/conformance`, `766b58be`/`12f5cecd`) | implemented | Mirrored `builder_headers`, `clob_auth_v2`, `ctf_calldata`, and `order_v2_poly1271` into `test/fixtures/conformance`; `test/conformance/golden_conformance_test.dart` locks builder/L2 headers, local-private-key L1 EOA signatures, Order V2 struct hashes, and CTF calldata byte-for-byte. |
| Sync parity matrix tooling | implemented | `docs/POLYDART-POLYGOLEM-PARITY.md` is generated from `docs/parity/polydart-polygolem.yaml` by `tool/generate_parity_docs.dart` (Dart, matching the pure-Dart toolchain); `dart run tool/generate_parity_docs.dart --check` runs in CI to catch a stale matrix |
